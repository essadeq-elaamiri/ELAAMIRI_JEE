package com.elaamiri.gestionpatients.web;

import org.springframework.stereotype.Controller;

@Controller
public class PatientController {
    // constructor injection
    // use @AllArgsConstructor of lombok

    //@GetMapping(path="") returns the html view 'view_name' (/resources/template)
}
